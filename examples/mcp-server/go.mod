module craftplan-mcp

go 1.23

// Run `go mod tidy` to resolve and pin github.com/mark3labs/mcp-go and
// github.com/jackc/pgx/v5 — intentionally left unpinned here so this example
// always picks up current releases instead of a version frozen at write-time.
