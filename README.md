# harness-kit
Production-ready harness for Claude Code: spec-driven workflow (/spec → /plan → /verify → /retro), subagents, deny-by-default permissions, security hooks, and eval regression testing. One install script, three adoption scenarios: greenfield, onboarding, existing repo.
