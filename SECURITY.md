# Security Policy

Do not open public issues for exploitable problems (gate bypasses,
secrets-scan evasion, injection via trace files). Use GitHub's private
vulnerability reporting on this repository: Security tab → Report a vulnerability.

Supported version: latest tagged release only.